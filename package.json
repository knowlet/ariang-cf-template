{
	"name": "ariang-cf-template",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"upgrade": "gh release download --repo mayswind/AriaNg --pattern 'AriaNg-*-AllInOne.zip' -O tmp.zip && unzip -jo tmp.zip -d ./public && rm tmp.zip"
	},
	"devDependencies": {
		"wrangler": "^4.34.0"
	}
}